function InfoCard({ title, text }) {
    return (
        <article className="example-box">
            <h4>{title}</h4>
            <p>{text}</p>
        </article>
    );
}

export default InfoCard;
