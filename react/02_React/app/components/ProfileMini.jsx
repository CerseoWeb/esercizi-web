function ProfileMini({ name, role }) {
    return (
        <article className="example-box">
            <h4>{name}</h4>
            <p>{role}</p>
        </article>
    );
}

export default ProfileMini;
